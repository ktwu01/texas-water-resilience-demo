# Core runtime. Install with: pip install -r requirements.txt
# or, preferred: pip install -e ".[dev]"
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
PyYAML>=6.0
matplotlib>=3.7

# Dashboard. Not needed for the pipeline or the static report, which
# scripts/make_report.py renders without it. It is uncommented here rather than
# left as an extra because Streamlit Community Cloud installs this file and only
# this file: commenting it out means the hosted app fails to start. For a
# pipeline-only install use `pip install -e .`, which keeps it an extra.
streamlit>=1.49   # width="stretch" replaced the removed use_container_width

# Development
# pytest>=7.4
# ruff>=0.4
