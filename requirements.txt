# Core runtime. Install with: pip install -r requirements.txt
# or, preferred: pip install -e ".[dev]"
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
PyYAML>=6.0
matplotlib>=3.7

# Optional interactive dashboard. scripts/make_report.py renders the same
# content as static HTML without it.
# streamlit>=1.30

# Development
# pytest>=7.4
# ruff>=0.4
